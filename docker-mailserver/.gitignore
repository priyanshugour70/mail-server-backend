# Docker-Mailserver .gitignore

# Environment variables
.env
.env.local
.env.production

# Mail data (do not commit actual mail data)
data/
state/

# Config files with sensitive data
config/postfix-accounts.cf
config/postfix-virtual.cf
config/dovecot-masters.cf
config/dovecot-quotas.cf

# Logs
*.log

